<style>
  .flatpickr-range-labels {
    text-align: center;
    padding: 10px 12px 8px;
    background: linear-gradient(180deg, rgba(99, 102, 241, 0.14) 0%, rgba(99, 102, 241, 0.06) 100%);
    border-bottom: 1px solid rgba(99, 102, 241, 0.3);
  }
  .flatpickr-range-labels .fp-range-legend-row {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.5rem 0.75rem;
    font-size: 0.9rem;
    font-weight: 600;
  }
  .flatpickr-range-labels .fp-legend-pickup { color: #c7d2fe; }
  .flatpickr-range-labels .fp-legend-return { color: #93c5fd; }
  .flatpickr-range-labels .fp-legend-arrow {
    color: #94a3b8;
    opacity: 0.95;
    font-weight: 400;
  }
  .flatpickr-range-labels .fp-legend-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.35rem;
    height: 1.35rem;
    border-radius: 6px;
    font-size: 0.68rem;
    margin-right: 6px;
    font-weight: 700;
    vertical-align: middle;
  }
  .flatpickr-range-labels .fp-mark-pickup {
    background: #6366f1;
    color: #fff;
  }
  .flatpickr-range-labels .fp-mark-return {
    background: #3b82f6;
    color: #fff;
  }
  .flatpickr-range-labels .fp-range-legend-hint {
    margin-top: 6px;
    font-size: 0.74rem;
    font-weight: 400;
    color: #a5adc4;
    line-height: 1.35;
  }
</style>
